export class Config {
    /** 
     * @param {string} theme
     * @param {boolean} darkmode 
     */
    constructor(theme, darkmode) {
        this.darkmode = darkmode;
        this.theme = theme;
    }
};
